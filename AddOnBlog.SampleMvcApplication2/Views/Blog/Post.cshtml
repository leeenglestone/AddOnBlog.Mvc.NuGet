@model AddOnBlog.MvcApplication.Models.PostViewModel
@{
    ViewBag.Title = "Post";
    Layout = "~/Views/Shared/_LayoutBlog.cshtml";
}

<div class="container body-content">

    @*<div class="blog-header">
        <h1 class="blog-title">@Model.Post.Title</h1>
        <p class="lead blog-description">Some sort of blog sub title here.</p>
    </div>*@

    <div class="row">
        <div class="col-sm-12 blog-main">
            @Html.Partial("_BlogPost", Model)


            @if(User.Identity.IsAuthenticated)
            {
            <hr />
            <div id="post-actions">
                <p>@Html.ActionLink("Edit", "Edit", new { id = Model.Post.Id }, new { @class="btn btn-warning" })</p>
            </div>
            }

        </div>
    </div>
</div>

